@Model iDunnoThePlayerProbably

<section class="buttonsList flex flex-horizontal">
    <header>
        <h3>Actions</h3> <!-- We should get this from a 'resources' file -->
    </header>

    @foreach (ActiveEffectModelOrSomething action in Model.ActiveEffects) {
        @await Html.PartialAsync("Button.cshtml", action);
    }
</section>